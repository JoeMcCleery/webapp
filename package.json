{
  "name": "webapp",
  "version": "1.0.0",
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^6.0.0",
    "prettier": "^3.7.4",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "syncpack": "alpha",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": "24.12.0"
  },
  "packageManager": "pnpm@10.26.1",
  "private": true,
  "scripts": {
    "syncpack:fix": "syncpack fix --dependency-types dev,peer,prod,engines,packageManager && syncpack format",
    "syncpack:update": "syncpack update && syncpack format"
  }
}
